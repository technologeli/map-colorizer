import { Text, Title } from "@mantine/core";

const Controls = () => {
  return (
    <section id="controls">
      <Title order={2}>Controls</Title>
      <Text>The controls of the </Text>
    </section>
  );
};

export default Controls;
